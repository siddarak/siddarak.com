export const metadata = {
  title: "Sid Darak — campaign poster",
  description:
    "Print poster: Sid Darak for Communications Chair. Vote Sep 2–5.",
  robots: { index: false, follow: false },
};

export default function PosterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
